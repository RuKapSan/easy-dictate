# Easy Dictate

Easy Dictate — это десктопное приложение для транскрибации аудио и диктовки текста с использованием AI. Построено на Tauri с поддержкой OpenAI Whisper и Groq API.

## Возможности

- 🎤 **Запись аудио** — захват звука с микрофона по горячей клавише
- 🔤 **Транскрибация** — преобразование речи в текст через OpenAI Whisper или Groq
- 🤖 **AI обработка** — улучшение текста с помощью GPT или Groq LLM
- ⌨️ **Автоматический ввод** — вставка распознанного текста в активное приложение
- 🎯 **Системный трей** — работа в фоновом режиме с быстрым доступом
- ⚡ **Горячие клавиши** — настраиваемые комбинации для быстрого запуска записи

## Технологический стек

- **Backend**: Rust + Tauri
- **Frontend**: HTML/JavaScript
- **AI APIs**: OpenAI (Whisper, GPT), Groq (Whisper, LLM)
- **Аудио**: cpal для записи, hound для WAV

## Установка

### Требования

- Rust 1.77.2+
- Node.js 18+
- Windows/macOS/Linux

### Сборка из исходников

```bash
# Клонирование репозитория
git clone https://github.com/RuKapSan/easy-dictate.git
cd easy-dictate

# Установка зависимостей
cd src-tauri
cargo build --release

# Запуск в режиме разработки
cargo tauri dev

# Сборка приложения
cargo tauri build
```

## Настройка

### API ключи

Приложение поддерживает два провайдера:

#### OpenAI
1. Получите API ключ на [platform.openai.com](https://platform.openai.com)
2. Введите ключ в настройках приложения

#### Groq
1. Получите API ключ на [console.groq.com](https://console.groq.com)
2. Введите ключ в настройках приложения

### Горячие клавиши

По умолчанию: `Ctrl+Shift+Space`

Можно изменить в настройках приложения. Поддерживаемые модификаторы:
- Ctrl/Cmd
- Shift
- Alt/Option

## Использование

1. **Запуск приложения** — приложение запустится в системном трее
2. **Открытие настроек** — клик по иконке в трее → Settings
3. **Настройка API** — выберите провайдера и введите API ключ
4. **Начало записи** — нажмите горячую клавишу или кнопку в интерфейсе
5. **Диктовка** — говорите в микрофон
6. **Остановка** — нажмите горячую клавишу повторно
7. **Результат** — текст будет вставлен в активное приложение

## Архитектура

### Основные модули

#### Core (`src-tauri/src/core/`)
- **commands.rs** — Tauri команды для frontend
- **state.rs** — управление состоянием приложения
- **hotkey.rs** — обработка горячих клавиш
- **transcription.rs** — логика транскрибации
- **events.rs** — система событий
- **tray.rs** — системный трей

#### Провайдеры (`src-tauri/src/`)
- **openai.rs** — интеграция с OpenAI API
- **groq.rs** — интеграция с Groq Whisper
- **groq_llm.rs** — интеграция с Groq LLM

#### Утилиты
- **audio.rs** — запись и обработка аудио
- **input.rs** — эмуляция ввода текста
- **settings.rs** — управление настройками

### Frontend

Минималистичный интерфейс на чистом JavaScript:
- **index.html** — UI настроек
- **main.js** — логика взаимодействия с Tauri

## API команды

### Транскрибация
```javascript
// Старт записи
await invoke('start_recording')

// Остановка и транскрибация
await invoke('stop_and_transcribe')

// Проверка статуса
const isRecording = await invoke('is_recording')
```

### Настройки
```javascript
// Получение настроек
const settings = await invoke('get_settings')

// Сохранение настроек
await invoke('save_settings', { settings: {...} })

// Валидация API ключа
const isValid = await invoke('validate_api_key', {
  provider: 'openai',
  apiKey: 'sk-...'
})
```

### Горячие клавиши
```javascript
// Регистрация горячей клавиши
await invoke('register_hotkey', { hotkey: 'Ctrl+Shift+Space' })

// Валидация комбинации
const isValid = await invoke('validate_hotkey', { hotkey: 'Ctrl+Space' })
```

## События

Приложение использует систему событий для коммуникации:

```javascript
// Подписка на события статуса
listen('transcription-status', (event) => {
  console.log('Phase:', event.payload.phase)
  console.log('Message:', event.payload.message)
})
```

Фазы транскрибации:
- `recording` — идёт запись
- `processing` — обработка аудио
- `transcribing` — отправка на сервер
- `enhancing` — улучшение текста (опционально)
- `complete` — готово
- `error` — ошибка

## Разработка

### Запуск dev сервера

```bash
cargo tauri dev
```

### Логирование

Приложение использует `tauri-plugin-log` для логирования. Логи сохраняются в:
- Windows: `%APPDATA%\app\logs`
- macOS: `~/Library/Logs/app`
- Linux: `~/.config/app/logs`

### Тестирование

```bash
cargo test
```

## Лицензия

MIT

## Вклад

Приветствуются pull requests. Для больших изменений сначала откройте issue.

## Поддержка

Создайте issue на GitHub для сообщения об ошибках или предложений.
