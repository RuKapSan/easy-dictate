# План реализации пожеланий пользователя

## Дата: 2025-12-10
## Версия: v0.1.0

---

## Запрошенные фичи

### 1. Запуск программы в фоне без окна

**Проблема:** Пользователь хочет, чтобы программа запускалась сразу в трее без показа главного окна.

**Текущее поведение:**
- При обычном запуске показывается главное окно с настройками
- При запуске с флагом `--autostart` окно скрыто
- Системный трей уже реализован

**Решение:** Добавить настройку `start_minimized`

**Реализация:**
- Добавить поле `start_minimized: bool` в `AppSettings` (по умолчанию `false`)
- Модифицировать логику в `lib.rs`: `if !is_autostart && !initial.start_minimized { window.show() }`
- Добавить чекбокс в UI настроек

**Файлы:**
- `src-tauri/src/settings.rs` - добавить поле
- `src-tauri/src/lib.rs` - изменить логику запуска
- `frontend/index.html` - добавить UI
- `frontend/main.js` - обработка настройки

**Сложность:** Простая (2-3 часа)

---

### 2. Горячая клавиша для переключения "переводить результат"

**Проблема:** Пользователь работает в двуязычном режиме и хочет быстро переключаться между режимами "с переводом" и "без перевода".

**Текущее поведение:**
- Настройка `auto_translate` существует
- Чтобы переключить, нужно открыть окно настроек → найти чекбокс → сохранить

**Варианты решения:**
1. Отдельная горячая клавиша для "режима с переводом"
2. **Toggle hotkey - переключатель настройки** ✅ (рекомендуется)
3. Модификатор к основной клавише

**Решение:** Toggle hotkey (например `Ctrl+Alt+T`)

**Реализация:**
```rust
// В AppSettings
pub toggle_translate_hotkey: String,  // "Ctrl+Alt+T"

// Новая команда
#[tauri::command]
async fn toggle_auto_translate(state: State<'_, AppState>) -> Result<bool> {
    let mut settings = state.current_settings().await;
    settings.auto_translate = !settings.auto_translate;
    state.update_settings(settings.clone()).await?;
    Ok(settings.auto_translate)
}

// В hotkey.rs - регистрация второй горячей клавиши
// В tray.rs - обновление статуса: "Status: Idle (Translate: ON)"
```

**Особенности:**
- Состояние сохраняется между сессиями
- Статус отображается в системном трее
- Обновление меню трея при переключении

**Файлы:**
- `src-tauri/src/settings.rs` - добавить `toggle_translate_hotkey`
- `src-tauri/src/core/hotkey.rs` - регистрация hotkey
- `src-tauri/src/core/commands.rs` - команда `toggle_auto_translate`
- `src-tauri/src/core/tray.rs` - обновление статуса
- `src-tauri/capabilities/default.json` - разрешение для команды
- `frontend/index.html` - UI для hotkey
- `frontend/main.js` - обработка

**Сложность:** Средняя (6-8 часов)

---

### 3. Окно с историей последней реплики (русский + английский)

**Проблема:** Пользователь хочет:
- Видеть последние транскрибированные фразы
- Видеть оригинал И перевод одновременно
- Использовать это для изучения языка

**Текущее поведение:**
- Результат показывается временно в `#last-result`
- Сразу вставляется/копируется в буфер
- История не сохраняется

**Решение:** Секция истории в главном окне

**UI дизайн:**
```
┌──────────────────────────────────────────┐
│ История транскрипций                     │
│                      [Очистить историю]  │
├──────────────────────────────────────────┤
│ 🕐 12:45:32                              │
│ 🇷🇺 Привет как дела                      │
│ 🇬🇧 Hello how are you                    │
├──────────────────────────────────────────┤
│ 🕐 12:43:10                              │
│ 🇷🇺 Это тестовая фраза                   │
│ 🇬🇧 This is a test phrase                │
├──────────────────────────────────────────┤
│ 🕐 12:40:05                              │
│ 🇷🇺 Диктовка без перевода                │
│    (перевод не выполнялся)               │
└──────────────────────────────────────────┘
```

**Структура данных:**
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct HistoryEntry {
    pub timestamp: String,          // "12:45:32" или ISO format
    pub original_text: String,      // Оригинальный распознанный текст
    pub translated_text: Option<String>,  // Если был перевод
    pub was_translated: bool,       // Флаг попытки перевода
}

// В AppState
history: Arc<Mutex<Vec<HistoryEntry>>>,  // Последние 50 записей
```

**Реализация:**
1. Добавить `history` в `AppState`
2. В `transcription.rs` - сохранять результаты в историю перед вставкой
3. Команды: `get_history()`, `clear_history()`
4. UI секция в `index.html` после статуса
5. Event listener для обновления истории в реальном времени

**Edge cases:**
- Перевод отключен → показывать только оригинал
- Перевод не удался → показывать ошибку или "(перевод не выполнен)"
- Очень длинный текст → обрезать или скролл
- Кастомные инструкции → показывать обработанный результат

**Файлы:**
- `src-tauri/src/core/state.rs` - добавить `history: Vec<HistoryEntry>`
- `src-tauri/src/core/transcription.rs` - сохранение в историю
- `src-tauri/src/core/commands.rs` - `get_history`, `clear_history`
- `src-tauri/capabilities/default.json` - разрешения
- `frontend/index.html` - UI секция
- `frontend/main.js` - рендеринг истории
- `frontend/styles.css` - стили для истории

**Сложность:** Средне-высокая (10-12 часов)

---

### 4. Автоматическая очистка истории раз в неделю

**Проблема:** История может расти бесконечно и занимать память/место.

**Решение:** Настраиваемая автоочистка

**Реализация:**
```rust
// В AppSettings
pub auto_clear_history: bool,     // По умолчанию true
pub clear_history_days: u32,      // По умолчанию 7

// В state.rs или отдельном файле
pub struct HistoryManager {
    last_clear_timestamp: Option<SystemTime>,
}

// В lib.rs - фоновая задача
tokio::spawn(async move {
    loop {
        tokio::time::sleep(Duration::from_secs(86400)).await; // Проверка раз в день

        if settings.auto_clear_history {
            let days_since_clear = /* вычислить */;
            if days_since_clear >= settings.clear_history_days {
                state.clear_history().await?;
                state.update_last_clear_timestamp().await?;
                log::info!("[History] Auto-cleared history after {} days", days_since_clear);
            }
        }
    }
});
```

**UI:**
```html
<label class="switch-row">
  <input type="checkbox" id="autoClearHistory" checked />
  <span>Автоматически очищать историю</span>
</label>
<label id="clearIntervalWrapper">
  Очищать каждые
  <select id="clearHistoryDays">
    <option value="1">1 день</option>
    <option value="7" selected>7 дней</option>
    <option value="30">30 дней</option>
    <option value="0">Никогда</option>
  </select>
</label>
<small class="hint">Последняя очистка: 3 дня назад</small>
```

**Особенности:**
- Ручная очистка сбрасывает таймер
- При отключенной автоочистке история сохраняется
- Уведомление пользователя (опционально)

**Файлы:**
- `src-tauri/src/settings.rs` - добавить поля
- `src-tauri/src/lib.rs` - фоновая задача
- `src-tauri/src/core/state.rs` - логика очистки, хранение timestamp
- `frontend/index.html` - UI настроек
- `frontend/main.js` - обработка

**Сложность:** Простая-средняя (3-4 часа)

---

## План реализации

### Фаза 1: Основа (12-15 часов)
**Приоритет:** Высокий

1. **Feature 1: Start minimized** (2-3 часа)
   - Независимая фича
   - Быстрая победа
   - Улучшает UX сразу

2. **Feature 3: История транскрипций** (10-12 часов)
   - Фундамент для Feature 4
   - Основная ценность для пользователя
   - Интеграция с существующим кодом

**Результат:** Пользователь может запускать в фоне и видеть историю фраз

---

### Фаза 2: Улучшение workflow (6-8 часов)
**Приоритет:** Средний

1. **Feature 2: Toggle hotkey для перевода** (6-8 часов)
   - Улучшает удобство работы
   - Интегрируется с историей
   - Полезно для двуязычных пользователей

**Результат:** Быстрое переключение режима перевода без открытия настроек

---

### Фаза 3: Maintenance (3-4 часа)
**Приоритет:** Низкий

1. **Feature 4: Автоочистка истории** (3-4 часа)
   - Зависит от Feature 3
   - Nice-to-have
   - Предотвращает разрастание данных

**Результат:** Автоматическое управление историей

---

## Общая оценка

**Общее время:** 21-27 часов чистой разработки

**Breakdown:**
- Rust backend: ~60% времени
- Frontend UI/JS: ~30% времени
- Тестирование: ~10% времени

**Риски:**
- Хранение истории может замедлить UI при большом объёме
- Hotkey может конфликтовать с системными
- Кроссплатформенные различия в поведении трея

**Тестирование:**
- Unit тесты для валидации настроек
- E2E тесты для hotkey и истории
- Ручное тестирование на Windows/Linux/macOS

---

## Зависимости между фичами

```
Feature 3 (История) ──┬──> Feature 4 (Автоочистка)
                      │
Feature 2 (Toggle) ───┴──> Интеграция с историей (отображение статуса)

Feature 1 (Minimized) ────> Независимая
```

---

## Технические детали

### Хранение истории

**Вариант 1:** В памяти (текущий план)
- Простота реализации
- Теряется при перезапуске
- Ограничение 50 записей

**Вариант 2:** В файле
- Персистентность
- Можно экспортировать
- Требует сериализации

**Рекомендация:** Начать с варианта 1, добавить персистентность позже если нужно.

### Безопасность

**Проблема:** История может содержать конфиденциальные данные.

**Решения:**
1. Опция отключить историю полностью
2. Шифрование файла истории (если будет персистентность)
3. Настройка автоочистки по умолчанию

---

## Альтернативные идеи (не запрошены, но обсуждались)

### Feature 2: Альтернативный вариант
Вместо toggle hotkey - **второй hotkey для "translate mode"**:
- `Ctrl+Shift+Space` - обычная диктовка
- `Ctrl+Shift+Alt+Space` - диктовка с переводом

**Преимущества:**
- Более явное управление
- Не нужно помнить текущий режим

**Недостатки:**
- Два hotkey комбо
- Сложнее для запоминания

### Feature 3: Альтернативный вариант
**Отдельное окно истории** вместо секции в главном окне.

**Преимущества:**
- Можно держать открытым рядом с работой
- Больше места для истории

**Недостатки:**
- Ещё одно окно для управления
- Более сложная реализация

---

## Next Steps

1. Получить подтверждение от пользователя на план
2. Начать с Фазы 1 (Features 1 + 3)
3. Провести код-ревью после каждой фазы
4. Добавить E2E тесты для новых фич
5. Обновить документацию

---

**Создано:** 2025-12-10
**Автор:** Claude Code + User
**Статус:** Черновик, ожидает подтверждения
